# Repository Guidance

このリポジトリは Python の学習記録です。変更は既存の章ファイルに合わせて、小さく読みやすく保ってください。

## Editor Settings

- `editor.quickSuggestions` は、コードの動作変更と混同しないよう、通常は変更しない。
- 補完の挙動を変更する必要がある場合は、ソースコードへ設定を書き込まず、ワークスペース設定として明示する。
- Python の編集では、文字列内やコメント内の補完を無効化するなど、対象を限定した設定を優先する。

## Python Changes

- 実行確認には、利用可能な Python インタープリターで対象ファイルを構文チェックする。
- 学習用コードの意図を保ち、依頼されていないリファクタリングや依存関係の追加は行わない。